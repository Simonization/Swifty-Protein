{
  "name": "swifty-proteins-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Swifty-Proteins backend API (Fastify) — auth, ligand data, element reference.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/jwt": "^10.1.0",
    "argon2": "^0.41.1",
    "fastify": "^5.1.0"
  }
}
