{
  "name": "swifty-proteins-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Swifty-Proteins backend API (Fastify) — auth only; ligand data and element reference live in the app.",
  "engines": {
    "node": ">=20.12"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --env-file-if-exists=.env --watch src/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/helmet": "^13.1.0",
    "@fastify/jwt": "^10.1.0",
    "@fastify/rate-limit": "^11.2.0",
    "argon2": "^0.41.1",
    "fastify": "^5.1.0",
    "pg": "^8.22.0"
  }
}
