# Copy to .env and adjust for your machine.
#
# EXPO_PUBLIC_* vars are inlined into the JS bundle at build time (Expo convention) —
# fine here since it's just a base URL, not a secret.
#
# This is only the DEFAULT. The in-app Settings screen overrides it at runtime and
# persists the choice, so a build never has to be recompiled to reach a different
# backend — set this if you want a convenient starting value, not because you must.
#
# `make apk` / `make ios` / `make up` all overwrite this line with this
# machine's current LAN IP (see ../scripts/ensure-env.sh) — for a physical
# device tethered to this machine, that's already correct and you don't need
# to touch it. Set it by hand only for the emulator/simulator cases below, or
# to point a build at a backend on a different machine.
#
# - iOS Simulator / Android Emulator: localhost usually works (Android emulator
#   also maps 10.0.2.2 -> host machine if localhost doesn't resolve).
# - Physical device: use your machine's LAN IP, e.g. http://192.168.1.20:3000
#   (must be on the same network as the backend; see `make up` in the repo root).
#   On a phone, "localhost" is the phone itself — it will never reach your laptop.
EXPO_PUBLIC_API_URL=http://localhost:3000
